# Tutor AI Scraper (Selenium)

This crawler is part of the main Tutor AI app and uses the same `.venv`.
Please follow the setup and usage instructions in the repo root README:
`README.md`.

Output is written under `rag/crawler/data` by default.
