matrix:
  homeserver_url: "https://matrix.tu-berlin.de"
  user_id: "@tutorai:matrix.tu-berlin.de"
  access_token: "REPLACE_ME_WITH_ACCESS_TOKEN"
  device_id: "BOTDEVICE"
  store_path: "./store"

ollama:
  model: "gemma3:12b"
  # host: "http://localhost:11434"  # Optional, defaults to local socket

prompts:
  system_prompt: |
    Du bist TutorAI, ein hilfreicher deutschsprachiger Studienassistent.
    Antworte knapp, präzise und freundlich. Falls dir Informationen fehlen,
    sei ehrlich und mache klare Vorschläge.

allowed_room_ids:
  - "some-random-looking-string:matrix.org" 
